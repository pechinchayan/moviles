<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jhams.loginbonito">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="JUEGO"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registrar" />
        <activity android:name=".Juego" />
        <activity android:name=".Loggeado"/>
        <activity android:name=".AgregarObjeto"/>
        <activity android:name=".ModificarObjeto"/>
        <activity android:name=".MActivity">

        </activity>//

    </application>

</manifest>